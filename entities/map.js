module.exports = class Map {
    constructor(mapId, userId, centerLocation, name) {
        this.mapId = mapId;
        this.userId = userId;
        this.centerLocation = centerLocation;
        this.name = name;
    }
}